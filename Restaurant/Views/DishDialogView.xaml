<Window x:Class="Restaurant.Views.DishDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Restaurant.Views"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Dish Editor" Height="700" Width="850"
        Background="#F7F7F7">

    <Window.Resources>
        <!-- Culori personalizate din EmployeeView -->
        <Color x:Key="PrimaryColor">#FF3E8EDE</Color>
        <Color x:Key="AccentColor">#FF58C3B6</Color>
        <Color x:Key="BackgroundColor">#FFF6F9FC</Color>
        <Color x:Key="TextColor">#FF1E1E1E</Color>
        <Color x:Key="HeaderColor">#FF2A2A2A</Color>

        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
        <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}"/>
        <SolidColorBrush x:Key="HeaderBrush" Color="{StaticResource HeaderColor}"/>

        <!-- Stil pentru TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="False" 
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          HorizontalScrollBarVisibility="Hidden" 
                                          VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Stil îmbunătățit pentru ComboBox -->
        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="30"/>
                            </Grid.ColumnDefinitions>

                            <ToggleButton x:Name="ToggleButton" 
                                  Background="{TemplateBinding Background}"
                                  BorderBrush="{TemplateBinding BorderBrush}"
                                  BorderThickness="{TemplateBinding BorderThickness}"
                                  Grid.ColumnSpan="2"
                                  Focusable="false"
                                  IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                  ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Border" 
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="4">
                                            <Path x:Name="Arrow"
                                          Fill="{StaticResource TextBrush}"
                                          HorizontalAlignment="Right"
                                          Margin="0,0,10,0"
                                          VerticalAlignment="Center"
                                          Data="M 0 0 L 4 4 L 8 0 Z"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="BorderBrush" TargetName="Border" Value="{StaticResource AccentBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite"
                                      IsHitTestVisible="False" 
                                      Content="{TemplateBinding SelectionBoxItem}"
                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                      ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                      Margin="10,0,5,0"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Left"
                                      Grid.Column="0"/>
                            <Popup x:Name="Popup"
                           Placement="Bottom"
                           IsOpen="{TemplateBinding IsDropDownOpen}"
                           AllowsTransparency="True" 
                           Focusable="False"
                           PopupAnimation="Slide">
                                <Grid x:Name="DropDown"
                              SnapsToDevicePixels="True"                
                              MinWidth="{TemplateBinding ActualWidth}"
                              MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                    Background="White"
                                    BorderThickness="1"
                                    BorderBrush="{StaticResource PrimaryBrush}"
                                    CornerRadius="4">
                                        <Border.Effect>
                                            <DropShadowEffect Color="Gray" BlurRadius="8" ShadowDepth="2" Opacity="0.3"/>
                                        </Border.Effect>
                                    </Border>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Stil pentru ComboBoxItem -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border" 
                        Background="{TemplateBinding Background}"
                        Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter Property="Background" TargetName="Border" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="TextElement.Foreground" TargetName="Border" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" TargetName="Border" Value="#FFE3F2FD"/>
                                <Setter Property="TextElement.Foreground" TargetName="Border" Value="{StaticResource TextBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Stil pentru Button -->
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                               BorderBrush="{TemplateBinding BorderBrush}" 
                               BorderThickness="{TemplateBinding BorderThickness}" 
                               CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Gray" BlurRadius="5" ShadowDepth="1"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- Icon Button Style -->
        <Style x:Key="IconButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#F44336"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Content" Value="×"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource BackgroundBrush}" CornerRadius="0">
        <Grid Margin="25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="237*"/>
                <ColumnDefinition Width="96*"/>
                <ColumnDefinition Width="467*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Main Content Section -->
            <Border Grid.Row="0" Background="White" 
                    CornerRadius="8" Padding="25" Margin="0,0,0,20"
                    BorderBrush="{StaticResource PrimaryBrush}" BorderThickness="1" Grid.ColumnSpan="3">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Dish Details" Foreground="{StaticResource HeaderBrush}" 
                               FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Left Column - Basic Information -->
                        <StackPanel Grid.Column="0" Margin="0,0,15,0">
                            <TextBlock Text="Basic Information" Foreground="{StaticResource HeaderBrush}" 
                                       FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15"/>

                            <!-- Name -->
                            <TextBlock Text="Dish Name" Foreground="{StaticResource TextBrush}" 
                                       FontWeight="Medium" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding Dish.Name, UpdateSourceTrigger=PropertyChanged}" 
                                     Margin="0,0,0,15"/>

                            <!-- Price -->
                            <TextBlock Text="Price" Foreground="{StaticResource TextBrush}" 
                                       FontWeight="Medium" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding Dish.Price, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}" 
                                     Margin="0,0,0,15"/>

                            <!-- Category -->
                            <TextBlock Text="Category" Foreground="{StaticResource TextBrush}" 
                                       FontWeight="Medium" Margin="0,0,0,5"/>
                            <ComboBox Margin="0,0,0,15"
                                      DisplayMemberPath="Name"
                                      SelectedValuePath="CategoryId"
                                      SelectedValue="{Binding CategoryId, Mode=TwoWay}"
                                      ItemsSource="{Binding DataContext.Categories, RelativeSource={RelativeSource AncestorType=Window}}"/>
                        </StackPanel>

                        <!-- Right Column - Quantity Information -->
                        <StackPanel Grid.Column="1" Margin="15,0,0,0">
                            <TextBlock Text="Quantity Information" Foreground="{StaticResource HeaderBrush}" 
                                       FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15"/>

                            <!-- Quantity Per Portion -->
                            <TextBlock Text="Quantity Per Portion" Foreground="{StaticResource TextBrush}" 
                                       FontWeight="Medium" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding Dish.QuantityPerPortion, UpdateSourceTrigger=PropertyChanged}" 
                                     Margin="0,0,0,15"/>

                            <!-- Total Quantity -->
                            <TextBlock Text="Total Quantity" Foreground="{StaticResource TextBrush}" 
                                       FontWeight="Medium" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding Dish.TotalQuantity, UpdateSourceTrigger=PropertyChanged}" 
                                     Margin="0,0,0,15"/>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>

            <!-- Allergens Section -->
            <Border Grid.Row="1" Background="White" 
                    CornerRadius="8" Padding="25" 
                    BorderBrush="{StaticResource PrimaryBrush}" BorderThickness="1" Grid.ColumnSpan="3">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Allergens" Foreground="{StaticResource HeaderBrush}" 
                               FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

                    <!-- Add Allergen Control -->
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <ComboBox Grid.Column="0" 
                                  DisplayMemberPath="Name"
                                  SelectedItem="{Binding SelectedAllergen, Mode=TwoWay}"
                                  ItemsSource="{Binding DataContext.Allergens, RelativeSource={RelativeSource AncestorType=Window}}"/>

                        <Button Grid.Column="1" Content="Add Allergen" Margin="10,0,0,0" 
                                Click="AddAllergen_Click"/>
                    </Grid>

                    <!-- Allergens List -->
                    <Border Grid.Row="2" Background="#FFEFF4FA" CornerRadius="8" 
                            Margin="0,15,0,0" BorderBrush="{StaticResource PrimaryBrush}" 
                            BorderThickness="1" Padding="15">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding SelectedAllergens}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="White" CornerRadius="4" 
                                                BorderBrush="{StaticResource PrimaryBrush}" 
                                                BorderThickness="1" Margin="0,0,0,8" 
                                                Padding="12">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0" Text="{Binding Name}" 
                                                           VerticalAlignment="Center" 
                                                           FontWeight="Medium" 
                                                           Foreground="{StaticResource TextBrush}"/>

                                                <Button Grid.Column="1" Style="{StaticResource IconButtonStyle}" 
                                                        Click="RemoveAllergen_Click"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </Border>

            <!-- Footer Controls -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" 
                        HorizontalAlignment="Right" Margin="0,20,0,0" Grid.Column="2">
                <Button Content="Cancel" Style="{StaticResource SecondaryButtonStyle}" 
                        Width="100" Margin="0,0,10,0" IsCancel="True"/>
                <Button Content="Save Dish" Width="100" 
                        IsDefault="True" Click="Ok_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>